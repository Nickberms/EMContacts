<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.screens.contactPage"
        android:label="Home"
        tools:layout="@layout/HomeScreen">
        <action
            android:id="@+id/action_homeFragment_to_newPageScreen"
            app:destination="@id/newPageScreen" />
    </fragment>
    <fragment
        android:id="@+id/newPageScreen"
        android:name="com.example.NewPageScreen"
        android:label="New Page"
        tools:layout="@layout/your_layout_for_preview" />
</navigation>